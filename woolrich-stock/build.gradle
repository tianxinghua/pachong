dependencies {
    compile(

            "net.sourceforge.javacsv:javacsv:2.0",
            project(':iog-common-product'),
            project(':iog-sop')
    )
    testCompile group: 'junit', name: 'junit', version: '4.11'
}

jar {

    dependsOn copyToLib,copyResources, dependsClassPath
    exclude ("**/*.properties")
//    def manifestCp = configurations.compile.files.collect {
//        File file = it
//        "lib/${file.name}"
//    }.join(' ')

    manifest {
		attributes 'Main-Class': 'com.shangpin.iog.woolrich.stock.schedule.Startup'
//        attributes 'Main-Class': 'com.shangpin.iog.woolrich.stock.WoolrichStockImp'
//        attributes 'Class-Path': '. ./conf/  ' + manifestCp
    }
}
