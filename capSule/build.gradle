
dependencies {
    compile(
      'org.apache.poi:poi:3.13',
      'org.apache.poi:poi-excelant:3.13',
      project(':project-common'),
      project(':iog-common-product'),
      "commons-dbcp:commons-dbcp:1.4",
            
    )
    testCompile group: 'junit', name: 'junit', version: '4.11'
}

jar {

    dependsOn copyToLib , copyResources
    exclude ("**/*.properties")

    def manifestCp = configurations.compile.files.collect  {
        File file = it
        "lib/${file.name}"
    }.join(' ')

    manifest {
        attributes 'Main-Class': 'com.shangpin.iogcatalogue.StartUp'
        attributes 'Class-Path': '. ./conf/  ' +  manifestCp
    }
}