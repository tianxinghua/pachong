
dependencies {
    compile(
            project(':project-common'),
            //project(':openapi-ice')
            project(':iog-sop'),
    )
    //compile project(':dellogliostore')
    testCompile group: 'junit', name: 'junit', version: '4.11'
}

jar {
    dependsOn copyToLib , copyResources,dependsClassPath
    exclude ("**/*.properties")
  //  def manifestCp = configurations.compile.files.collect {
  //      File file = it
  //      "lib/${file.name}"
  //  }.join(' ')

    manifest {
        attributes 'Main-Class': 'com.shangpin.iog.dellogliostore.stock.GrabStockImp'
    //    attributes 'Class-Path': '. ./conf/  ' +  manifestCp
    }
}